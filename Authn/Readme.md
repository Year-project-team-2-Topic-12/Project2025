# MURA Classifier API Service

### Реализовано:
- аутентификацию на базе **JWT**,
- ролевую модель доступа (**RBAC**),
- персистентное хранение истории запросов,
- систему миграций базы данных.

---
### Безопасность и авторизация
- **JWT (JSON Web Tokens)** — доступ к защищённым эндпоинтам только с токеном.
- **Хеширование паролей** — пароли не хранятся в открытом виде (`bcrypt`).
- **Ролевая модель доступа (RBAC):**
  - 👮‍♂️ **Admin** — полный доступ, включая удаление истории запросов.
  - 👨‍💻 **User** — инференс и просмотр собственной истории.

---

## 📂 Структура проекта

```text
Authn/
├── alembic/                # Файлы миграций базы данных
├── routers/
│   ├── auth.py             # Эндпоинты авторизации (/auth/login)
│   └── history.py          # Эндпоинты истории (/history)
├── crud.py                 # CRUD-операции (пользователи, логи)
├── database.py             # Конфигурация подключения к БД
├── main.py                 # Точка входа FastAPI-приложения
├── models.py               # SQLAlchemy модели (User, RequestLog)
├── security.py             # Хеширование паролей и JWT-логика
├── create_admin.py         # Скрипт инициализации пользователей
├── requirements.txt        # Зависимости проекта
└── alembic.ini             # Конфигурация Alembic
```
---

## ⚙️ Установка и запуск

### 1️⃣ Подготовка окружения

Создайте виртуальное окружение:

```bash
python -m venv venv
```
Активируйте его:

**Windows:**
```bash
venv\Scripts\activate
```
**Linux / macOS:**
```bash
source venv/bin/activate
```
---

### 2️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```
---

### 3️⃣ Применение миграций БД

Создаст файл `history.db` и таблицы `users`, `request_logs`.

```bash
alembic upgrade head
```
---

### 4️⃣ Создание администратора

```bash
python create_admin.py
```
---

### 5️⃣ Запуск сервера

```bash
uvicorn main:app --reload
```
---

## 👥 Доступные пользователи

(создаются после запуска `create_admin.py`)

| Логин | Пароль | Роль | Права |
|------|--------|------|-------|
| admin | adminpass | Admin | Инференс, просмотр истории, удаление истории |
| user | user123 | User | Инференс, просмотр истории |

---

## 📘 Документация API

Swagger UI доступен по адресу:
```
http://127.0.0.1:8000/docs
```
---
## Авторизация в Swagger

1. Перейдите в `/docs`.
2. Нажмите **Authorize** (значок замка) или используйте эндпоинт `POST /auth/login`.
3. Введите `username` и `password`.
4. Получите `200 OK` с JWT-токеном.
5. Нажмите **Authorize** и вставьте токен.
После этого все защищённые эндпоинты будут доступны.

---
##  Эндпоинты API

### Аутентификация
- **POST `/auth/login`** — получение JWT-токена.

### 📜 История запросов
- **GET `/history`** — просмотр истории запросов (авторизация обязательна).
- **DELETE `/history`** — полная очистка истории (**только Admin**).

---


# 🏥 MURA Classifier API Service

Этот проект представляет собой **REST API сервис** для классификации медицинских снимков (MURA dataset). 

Сервис включает в себя полную инфраструктуру для промышленной эксплуатации: систему **аутентификации (JWT)**, ролевое управление доступом (**RBAC**), персистентное хранение истории запросов (**SQLite + SQLAlchemy**) и систему миграций (**Alembic**).

---

## 🚀 Основной функционал

### 1. 🧠 ML-Инференс
* Прием изображений через API.
* Обработка и классификация (имитация работы модели).
* Возврат результата с метаданными.

### 2. 🗄 Сохранение истории (Persistence)
* Все запросы к API автоматически логируются в базу данных `history.db`.
* Сохраняется: время выполнения, входные параметры, результат, статус-код.

### 3. 🔒 Безопасность и Авторизация
* **JWT (JSON Web Tokens):** Доступ к защищенным эндпоинтам только по токену.
* **Хеширование паролей:** Пароли не хранятся в открытом виде (используется `bcrypt`).
* **Ролевая модель:**
    * 👮‍♂️ **Admin:** Полный доступ + право удалять историю запросов.
    * 👨‍💻 **User:** Доступ к инференсу и просмотру своей истории.

---

## 🛠 Технический стек

* **Язык:** Python 3.9+
* **Фреймворк:** FastAPI + Uvicorn
* **База данных:** SQLite (для простоты) / PostgreSQL (поддерживается)
* **ORM:** SQLAlchemy
* **Миграции:** Alembic
* **Безопасность:** PyJWT, Passlib, Bcrypt

---

## 📂 Структура проекта

```text
mura_project/
├── alembic/                # Файлы миграций базы данных
├── routers/
│   ├── auth.py             # Эндпоинты авторизации (/login)
│   └── history.py          # Эндпоинты истории (/history)
├── crud.py                 # CRUD-операции (создание юзеров, чтение логов)
├── database.py             # Конфигурация подключения к БД
├── main.py                 # Точка входа приложения
├── models.py               # SQLAlchemy модели (User, RequestLog)
├── security.py             # Логика хеширования и проверки JWT-токенов
├── create_admin.py         # Скрипт инициализации пользователей
├── requirements.txt        # Зависимости проекта
└── alembic.ini             # Конфигурация Alembic

Установка и запуск
Следуйте этим шагам, чтобы запустить проект локально.

1.  Окружение

# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate
# Активация (Mac/Linux)
source venv/bin/activate

2. Установка зависимостей

pip install -r requirements.txt

3. Применение миграций БД
Создаст файл history.db и нужные таблицы (users, request_logs).

alembic upgrade head

4. Создание администратора

python create_admin.py

5. Запуск сервера

uvicorn main:app --reload

Доступные пользователи (после запуска create_admin.py)

Логин	Пароль	Роль	Права
admin	adminpass	Admin	Инференс, Просмотр истории, Удаление истории
User	user123	    User	Инференс, Просмотр истории

Документация API (Swagger UI)
Для тестирования перейдите на страницу: http://127.0.0.1:8000/docs

Как авторизоваться:
Найдите кнопку Authorize (справа сверху) или эндпоинт POST /auth/login.

Введите username и password из таблицы выше.

Получите ответ 200 OK с токеном.

Нажмите Authorize (зеленый замок) вверху страницы — теперь все запросы будут отправляться с вашим токеном.

Описание эндпоинтов:
POST /auth/login — Получение JWT-токена (вход в систему).

GET /history — Получение списка всех запросов (требуется авторизация).

DELETE /history — Полная очистка истории (требуется роль Admin).
